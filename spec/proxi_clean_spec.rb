require 'spec_helper'

describe ProxiClean do
  it 'has a version number' do
    expect(ProxiClean::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
